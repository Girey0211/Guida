# 로컬 개발용 시크릿 (wrangler dev / db:migrate 가 읽음). 실제 값으로 채워 .dev.vars 로 복사.
# Neon 대시보드 → 프로젝트 → Connection string (pooled, "Connection pooling" 켠 값 권장).
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx-pooler.REGION.aws.neon.tech/guida_db?sslmode=require
